# Core (memory_cli.py uses only stdlib — no deps needed)

# Evolution engine (python -m evolution.evolve_skill)
gepa>=0.3,<1.0
openai>=1.0,<2.0
click>=8.0,<9.0
rich>=13.0,<14.0    # optional, falls back to print
